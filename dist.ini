name = LLM::Agent

[ReadmeFromPod]
filename = docs/Readme.rakudoc

[UploadToZef]

; Deliberately off, as in LLM-Chat / JSONL / LLM-Data-Inference: `provides` is
; hand-maintained here. With the scanner on, an `mi6 build` run before the lib
; files are staged silently DROPS them from `provides`, and the resulting dist
; installs with missing modules.
[AutoScanPackages]
enabled = false

[Badges]
provider = github-actions/test.yml
